{% extends 'EpikaClubBundle::layout.html.twig' %}
<h1>Bono creation</h1>

<form action="{{ path('bono_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('bono') }}">
            Back to the list
        </a>
    </li>
</ul>
