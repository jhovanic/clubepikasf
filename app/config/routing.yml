EpikaClubBundle:
    resource: "@EpikaClubBundle/Controller/"
    type:     annotation
    prefix:   /

login_check:
    pattern: /login_check
    
logout:
    pattern: /logout

# Internal routing configuration to handle ESI
#_internal:
#   resource: "@FrameworkBundle/Resources/config/routing/internal.xml"
#   prefix:   /_internal
